= render 'alblum_list'

.span9.contentcolumn
  .row-fluid
    .span12
      .well
        .navbar.navbar-heading
          .navbar-inner
            .container{:style => "width: auto;"}
              %a.brand{:href => "#"}= @alblum.name

        = simple_form_for [:admin, @alblum],
          :html => {:multipart => true, :id => 'fileupload'} do |f|

          = render :partial => 'fileupload', :locals => {:f => f}

